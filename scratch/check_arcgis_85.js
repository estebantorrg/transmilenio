const fs = require('fs');

// Let's see if we have cached arcgis data or let's search in server/src/data/
// Wait, is there a JSON file or dump of the responses on disk?
// Let's list files in server/src/data or client/public or similar.
console.log('Listing files to find ArcGIS data...');
