server/src/data/master_catalog.json filter=lfs diff=lfs merge=lfs -text

# Gradle wrapper must stay LF (executed by /bin/sh on Linux CI runners) and the
# wrapper jar must stay binary regardless of the checkout OS.
mobile/android/gradlew text eol=lf
mobile/android/gradle/wrapper/gradle-wrapper.jar binary
