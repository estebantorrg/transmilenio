{
  "name": "transmilenio-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "TransMilenio Explorer - Android app. Capacitor shell around the web client; live tracking runs natively from the device's own Colombian connection.",
  "scripts": {
    "postinstall": "patch-package",
    "build:web": "node scripts/build-web.mjs",
    "sync": "npm run build:web && cap sync android",
    "apk": "npm run sync && cd android && gradlew assembleDebug",
    "open": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/app": "^6.0.2",
    "@capacitor/core": "^6.2.1",
    "phonegap-nfc": "^1.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.1",
    "patch-package": "^8.0.1"
  }
}
