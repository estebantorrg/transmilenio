node_modules/
dist/

.env
.env.*
!.env.example

*.log
.DS_Store
Thumbs.db

# local agent/session state
.claude/

# scratch & one-off dumps
scratch/
*_dump.json
*_dump.txt
live_buses_dump.json

# local-only reverse-engineering research (official app APK + capture
# tooling, spec §5.5.1b/§5.8) — never committed/redistributed, see research/README.md
research/

# mobile offline bundle — derived from committed server data by
# `npm run bundle:mobile` (server); regenerated on every `mobile` build:web (spec §5.2.1b)
client/mobile/src/generated/

# local-only source material — kept on disk, never committed. TRANSMILENIO S.A.
# publications we read from and cite but do not redistribute: the brand manual
# the rutero is checked against (spec §5.5.5) and the 210 route plegables. 342 MB
# of third-party PDFs would also be permanent history the Render deploy re-clones
# on every build (spec §5.1.3, 512 MB instance).
ManualDeIdentidad.pdf
plegables/

# derechos de petición drafted here — they carry the maintainer's own name and
# identity document, so they stay local (spec §3.3, zero PII).
peticiones/

# Playwright
playwright-report/
test-results/
blob-report/
playwright/.local-browsers/
